/*
1. CLUSTER(Mongo_Atlas_John) => DATABASE(Seller) => COLLECTION(members vh) => DOCUMENT(birdona data)
2. git log --oneline => oxirgi commitni ko'rish
3. git reset --hard => oxirgi commitga qaytish
*/
